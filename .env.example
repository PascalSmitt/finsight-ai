# Optional: turns on AI mode (Claude answers via tool calls over the same data)
ANTHROPIC_API_KEY=
FINSIGHT_MODEL=claude-sonnet-5

# Required by the SEC for the ingestion script: identify yourself
SEC_USER_AGENT=Your Name your.email@example.com

# Allow POST /api/refresh to re-pull filings from the SEC
ENABLE_REFRESH=0
